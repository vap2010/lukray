<p id="notice"><%= notice %></p>

<h1>
  <%= @page.name %>
</h1>

<%= simple_format h(@page.content) %>

<p>
<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
<%= link_to 'Destroy', @page, :cofirm => 'Are you sure?', :method => :delete %> |
</p>